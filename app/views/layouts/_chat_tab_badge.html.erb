<%# locals: count — số chưa đọc (chat hoặc thông báo). Wrapped in a stable id so a
    Turbo Stream broadcast can replace just this badge live. %>
<span id="chat_tab_badge">
  <% if count.to_i.positive? %><span class="ce-tab-badge"><%= count.to_i > 9 ? "9+" : count.to_i %></span><% end %>
</span>
