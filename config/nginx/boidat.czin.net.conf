upstream puma_boidat {
  server unix:/var/www/boidat/shared/tmp/sockets/puma.sock fail_timeout=0;
}

# Redirect HTTP → HTTPS (apex + all shop subdomains)
server {
  listen 80;
  server_name boidat.czin.net *.boidat.czin.net;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl http2;
  # Apex (super-admin, merchant, /w/:slug) + wildcard shop subdomains (white-label PWA)
  server_name boidat.czin.net *.boidat.czin.net;

  # SSL — managed by Certbot.
  #   Apex-only cert works for boidat.czin.net; for valid TLS on shop
  #   subdomains install a wildcard cert (*.boidat.czin.net via DNS-01) and
  #   point the two lines below at it.
  ssl_certificate     /etc/letsencrypt/live/boidat-wildcard/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/boidat-wildcard/privkey.pem;
  include             /etc/letsencrypt/options-ssl-nginx.conf;
  ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

  root /var/www/boidat/current/public;

  add_header X-Frame-Options        SAMEORIGIN;
  add_header X-Content-Type-Options nosniff;
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

  client_max_body_size 20M;

  gzip on;
  gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml application/manifest+json;
  gzip_min_length 1000;

  location ^~ /assets/ {
    gzip_static on;
    expires 1y;
    add_header Cache-Control public;
    break;
  }

  location ^~ /rails/active_storage/ {
    proxy_pass         http://puma_boidat;
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;
  }

  # ActionCable — chat realtime giữa admin và phụ huynh. THIẾU block này thì
  # WebSocket bị nginx từ chối âm thầm và tin nhắn chỉ hiện sau khi F5.
  location /cable {
    proxy_pass          http://puma_boidat;
    proxy_http_version  1.1;
    proxy_set_header    Upgrade $http_upgrade;
    proxy_set_header    Connection "upgrade";
    proxy_set_header    Host $host;
    proxy_set_header    X-Real-IP $remote_addr;
    proxy_set_header    X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header    X-Forwarded-Proto $scheme;
    proxy_read_timeout  3600s;
    proxy_send_timeout  3600s;
  }

  location @puma {
    proxy_pass         http://puma_boidat;
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header   X-Forwarded-Proto $scheme;
    proxy_redirect     off;
    proxy_read_timeout 120s;
  }

  try_files $uri @puma;
}

# Service nhận diện khuôn mặt KHÔNG có server block ở đây một cách có chủ đích:
# nó chỉ bind 127.0.0.1:8008 và chỉ Rails gọi tới. Dữ liệu sinh trắc học của trẻ
# em không được phép có đường ra Internet.
